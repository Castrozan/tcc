export class AboutImage {
    constructor(
        public id: number | null,
        public aboutId: number | null,
        public url: string | null,
        public title: string | null,
        public description: string | null
    ) {}
}
